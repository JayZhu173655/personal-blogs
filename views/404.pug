doctype html
html
  head
    title=title
    meta(charset="uft8")
    style(ref="stylesheet").
      .text-center{
        text-align: center;
      }
  body
    h1.text-center 404页面
    h2.text-center 你要找的页面去远行了，请耐心等待……

    div.text-center
      span#time 3
      |秒后将返回首页
    
    script.
      let n = 3
      const fn = () => {
        time.innerHTML = --n
        if(n === 0)location.href = "/"
      }
      setInterval(fn, 1000)
